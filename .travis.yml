language: python
python:
    - "2.7"

sudo: false
cache:
    directories:
        - "~/.platformio"

install:
    - pip install platformio==3.1.0 

script:
    - platformio test -e local | tail -n 1 | grep PASSED
